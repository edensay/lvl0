baignoire

- faire un formulaire en post sur la meme page
    - avec les information
        - contenu de la baignoir
        - remplisage en X ( plus mesure L, ml, ... )
        - ecoulement en X ( plus mesure L, ml, ... )
- a la soumission faire le calcule et les affichés

result = savoir si la baignoir va ce remplire ou non.


contenu ( numeric [ champs num ] ) - contenu ( mesure )[ select ]
remplisage ( numeric )[ champs num ] - remplisage ( mesure ) [ select ]  - temporalité ( min , ect . ) [ select ]
ecoulement ( numeric )[ champs num ]  - ecoulement ( mesure ) [ select ]  - temporalité ( min , ect . ) [ select ]
submit